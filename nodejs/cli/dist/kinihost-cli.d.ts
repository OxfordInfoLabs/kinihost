/**
 * Main kinihost CLI class
 */
export default class KinihostCli {
    private _handled;
    constructor(apiEndpoint: string, defaultConfigFilename: string, cliDisplayName: string, additionalCommands?: any[]);
    private initialise;
}
